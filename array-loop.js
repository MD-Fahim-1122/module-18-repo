var numbers =[20, 32, 40, 50, 111, 35, 50];

for (var i = 0; i < 7; i++) {
    var number = numbers[i];
    console.log(number)
}

var items = ['nailcutter', 'brush', 'pen', 'Headphone', 'mouse', 'notebook', 'cutter', 'cap', 'meril', 'keyboard', 'monitor', 'desktop']

for (var i = 0; i < items.length; i++) {
    var item = items[i];
    console.log(item)
}